name = ["刘备", "关羽", "张飞"]
name.insert(1, "阿斗")  # 在这个索引之前插入
print(name)

# name.insert(3, ("关平", "关索"))  # 将对象整体插入到指定位置
# name[3:3] = ["关平", "关索"]  # 将对象的元素，读取出来，插入到空切片锁定的位置
name[3:3] = ("关平", "关索")  # 将对象的元素，读取出来，插入到空切片锁定的位置
print(name)

name.append([["张苞","张绍"],["x","y"]])
print(name)